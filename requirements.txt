# NCERT Textbook Data Ingestion Pipeline
# Requirements for OCR and Text Extraction

# Core OCR dependencies
pytesseract==0.3.10        # Python wrapper for Tesseract OCR
Pillow==10.1.0             # Image processing library
pdf2image==1.16.3          # PDF to image conversion
opencv-python==4.8.1.78    # Advanced image preprocessing

# Tesseract language data (install separately)
# For Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
# For Linux: sudo apt-get install tesseract-ocr tesseract-ocr-eng tesseract-ocr-hin
# For macOS: brew install tesseract tesseract-lang

# Data processing
numpy==1.26.2              # Numerical operations
pandas==2.1.4              # Data manipulation (for batch analysis)

# Utilities
tqdm==4.66.1               # Progress bars
jsonschema==4.20.0         # JSON schema validation
python-dateutil==2.8.2     # Date handling

# Optional: Advanced features
# pytesseract-ocr==0.3.10  # Alternative Tesseract wrapper
# easyocr==1.7.0           # Alternative OCR engine (GPU accelerated)

# Development dependencies
pytest==7.4.3              # Testing framework
pytest-cov==4.1.0          # Coverage reporting
black==23.12.1             # Code formatting
flake8==6.1.0              # Linting
mypy==1.7.1                # Type checking

# System dependencies (not pip installable):
# - Tesseract OCR 5.x: https://github.com/tesseract-ocr/tesseract
# - Poppler (for pdf2image): https://poppler.freedesktop.org/
#   Windows: Download from https://github.com/oschwartz10612/poppler-windows/releases/
#   Linux: sudo apt-get install poppler-utils
#   macOS: brew install poppler

# Phase 3: Semantic Chunking
tiktoken>=0.5.1            # Accurate token counting for GPT models

# Phase 4: Embeddings and Vector Storage
sentence-transformers>=2.2.2  # Multilingual sentence embeddings
faiss-cpu>=1.7.4           # Fast vector similarity search (CPU-optimized)
scikit-learn>=1.3.0        # Similarity metrics and utilities

# Phase 5: Multi-Stage Retrieval Pipeline
scipy>=1.10.0              # For Spearman correlation in reranker analysis

# Phase 6: RAG Answer Generation (Optional LLM integration)
# openai>=1.0.0            # OpenAI API for GPT-4/GPT-3.5-turbo
# anthropic>=0.8.0         # Anthropic API for Claude
# together>=0.2.0          # Together AI for open-source models
